version: '3.8'
services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./squat_trainer.db:/app/squat_trainer.db
    environment:
      - FLASK_DEBUG=0
      - PORT=5000
